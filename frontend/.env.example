# Vite frontend environment variables
# Set the production API base URL (no trailing slash), e.g. https://api.yourdomain.com
VITE_API_BASE=

# Optional: development proxy target for vite dev server (e.g., http://localhost:5000)
# If set, vite will proxy /api and /uploads to this target during `npm run dev`.
DEV_API_TARGET=
